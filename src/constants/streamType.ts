export enum STREAM_STATUS {
  STREAMING = 'streaming',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
  SCHEDULED = 'scheduled',
}

export enum STREAM_DIRECTION {
  INCOMING = 'incoming',
  OUTGOING = 'outgoing',
}
